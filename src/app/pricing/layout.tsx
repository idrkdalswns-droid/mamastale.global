import type { Metadata } from "next";

export const metadata: Metadata = { title: "요금 안내 - mamastale" };

export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
