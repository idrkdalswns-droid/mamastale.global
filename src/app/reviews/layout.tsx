import type { Metadata } from "next";

export const metadata: Metadata = { title: "이용 후기 - mamastale" };

export default function ReviewsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
