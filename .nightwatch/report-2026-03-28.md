# 🏥 Nightwatch Report — 2026-03-28

## 건강 점수: 78/100

> 첫 리포트 (이전 비교 없음)

---

## Phase 1: 빌드 & 테스트

- 빌드: ✅ 성공
- 테스트: ✅ 340/340 통과 (14 파일)
- Lint: ⚠️ ESLint 미설정 (구성 프롬프트 발생)
- 타입 에러: ✅ 0건
- npm audit: ⚠️ 3건 (high 2, moderate 1)
  - Next.js 14: DoS 취약점 4건 (fix: Next.js 16 업그레이드 필요 — breaking change)
  - picomatch: ReDoS 취약점 (`npm audit fix`로 해결 가능)

---

## 🔴 즉시 수정 필요 (CRITICAL)

> 없음

---

## 🟡 주의 필요 (HIGH)

### **[F-001]** stories/[id] PATCH 수동 검증 (Zod 미사용)
- 파일: `src/app/api/stories/[id]/route.ts:157-209`
- 설명: PATCH 핸들러가 9개 optional 필드를 typeof로 수동 검증. Zod 스키마 미사용으로 타입 강제 변환 엣지 케이스 누락 가능
- 처방: `storyPatchSchema` Zod 스키마 정의 후 `safeParse()`로 교체

### **[F-001b]** TQ generate 라우트 UUID 미검증
- 파일: `src/app/api/tq/generate/route.ts:51`
- 설명: `session_id`를 query params에서 받아 null 체크만 수행, UUID 형식 검증 없음
- 처방: `isValidUUID(sessionId)` 체크 추가

### **[F-002]** site_settings 테이블 RLS 미적용
- 파일: `supabase/migrations/048_blind_system.sql`
- 설명: `site_settings` 테이블에 RLS가 활성화되지 않아, anon key로 기능 플래그(blind 시스템 등) 읽기/쓰기 가능
- 처방:
  ```sql
  ALTER TABLE site_settings ENABLE ROW LEVEL SECURITY;
  CREATE POLICY "Only service role" ON site_settings FOR ALL USING (false);
  ```

### **[F-003]** 활동지 미리보기 dangerouslySetInnerHTML XSS 벡터
- 파일: `src/components/teacher/worksheet/ResultStep.tsx` (dangerouslySetInnerHTML)
- 설명: 교사 활동지 HTML 미리보기에 AI 생성 HTML을 `dangerouslySetInnerHTML`로 렌더링. AI가 생성한 HTML에 스크립트가 포함될 경우 XSS 가능
- 처방: DOMPurify로 산화(sanitize) 후 주입하거나, sandboxed iframe 사용

---

## 🟢 개선 권장 (MEDIUM)

### **[F-004]** TQ submit 라우트 Claude API 타임아웃 부재
- 파일: `src/app/api/tq/submit/route.ts`
- 설명: Claude API 호출에 AbortSignal.timeout이 없어 응답 지연 시 커넥션이 무한 대기
- 처방: `AbortSignal.timeout(60_000)` 추가

### **[F-005]** 활동지 생성 Claude API 타임아웃 부재
- 파일: `src/app/api/teacher/worksheet/generate/route.ts:229-237`
- 설명: 활동지 생성 Claude 호출(2회 재시도)에 AbortController 없음
- 처방: `AbortSignal.timeout(60_000)` 추가

### **[F-006]** teacher verify-code 입력 수동 검증
- 파일: `src/app/api/teacher/verify-code/route.ts:60`
- 설명: body.code를 typeof로 수동 체크, Zod 스키마 미사용
- 처방: `z.object({ code: z.string().min(1).max(20) })` 스키마 적용

### **[F-007]** teacher stories POST 입력 수동 검증
- 파일: `src/app/api/teacher/stories/route.ts:138-145`
- 설명: title, spreads를 필드별 수동 검증, Zod 미사용
- 처방: Zod 스키마로 교체

### **[F-008]** Migration 참조 불일치 (community_likes/comments)
- 파일: `src/app/api/account/delete/route.ts` → `supabase/migrations/`
- 설명: 코드에서 `community_likes`/`community_comments` 테이블을 참조하지만, migration에서 CREATE TABLE/RENAME 기록 없음
- 처방: migration 파일 추가 또는 기존 migration 확인

### **[F-009]** Supabase client null 미체크
- 파일: `src/lib/supabase/client.ts:5-27` → 다수 호출처
- 설명: `createClient()`가 env 변수 없을 때 null 반환하지만, 호출처에서 null 가드 없이 체이닝
- 처방: 개발환경에서만 발생 가능. 프로덕션에선 env 필수이므로 LOW로 재분류 가능

### **[F-010]** push/send 라우트 Service Role Key 폴백
- 파일: `src/app/api/push/send/route.ts:68`
- 설명: PUSH_API_SECRET 미설정 시 SUPABASE_SERVICE_ROLE_KEY를 API 인증 키로 사용 — 헤더 탈취 시 DB 전체 접근 가능
- 처방: PUSH_API_SECRET 환경변수 필수화

---

## 📋 LOW 이슈 (8건, 요약)

| ID | 파일 | 설명 |
|----|------|------|
| F-011 | `.gitignore` | `.env` 패턴은 있으나 bare `.env` (확장자 없는) 미포함 |
| F-012 | `teacher/worksheet/[id]/route.ts` | Rate limiting 없음 (형제 라우트엔 있음) |
| F-013 | `tq/event/route.ts` | 응답 포맷 `{ ok: true }` — 프로젝트 표준 `{ error }` / `{ success }` 미준수 |
| F-014 | `test/cover/route.ts` | resolveUser 대신 직접 getUser 사용 (dev-only) |
| F-015 | `admin/refund/route.ts:204` | Slack 웹훅 fire-and-forget에 타임아웃 없음 |
| F-016 | `illustration/upload.ts:36` | 공유 createServiceRoleClient 미사용, 직접 클라이언트 생성 |
| F-017 | `referral/route.ts:181` | 동적 import로 admin 클라이언트 생성 (비표준 패턴) |
| F-018 | CSP `unsafe-inline` | nonce 기반 CSP로 전환 권장 |

---

## 📊 코드 품질 지표

| 항목 | 수치 | 평가 |
|------|------|------|
| `any` 타입 사용 | 9건 (7파일) | ✅ 양호 |
| Non-null assertions (`!.`) | 7건 (5파일, 테스트 제외) | ✅ 양호 |
| `console.log` 잔존 | 11건 | ⚠️ 일부 정리 필요 |
| TODO/FIXME | 미집계 | — |
| 미사용 export | ~92건 | ⚠️ 정리 필요 |
| 에러 응답 중복 | ~288건 (40 라우트) | ⚠️ 헬퍼 함수 도입 권장 |
| 테스트 커버리지 | 14파일/340 테스트 | ⚠️ API 라우트 통합 테스트 없음 |
| 번들 크기 위험 | framer-motion 30개 임포트 | ℹ️ v11+ `motion` 서브패스 전환 권장 |

---

## 🔒 보안 상태 (양호)

| 항목 | 상태 |
|------|------|
| 하드코딩 시크릿 | ✅ 없음 |
| .env git 추적 | ✅ 없음 |
| 프롬프트 인젝션 방어 | ✅ stripControlTags + sanitizeUserInput 적용 |
| 보안 헤더 | ✅ 전체 적용 (CSP, HSTS, X-Frame-Options 등) |
| 위기감지 CSSRS | ✅ 5단계 무결, 14개 false positive 필터 정상 |
| 출력 안전 필터 | ✅ 독성 긍정 + 의료 조언 필터 정상 |
| 결제 보안 | ✅ 서버사이드 금액 결정, 웹훅 서명 검증, 3중 멱등성 |
| 티켓 원자성 | ✅ RPC + CAS 폴백 (5회 재시도) |

---

## 💊 오늘의 처방전 (Top 3)

### 1. site_settings 테이블 잠금 (F-002)
**위험**: 지금 누구든 사이트 설정을 바꿀 수 있는 상태입니다. 마치 가게 금고에 자물쇠를 안 채운 것과 같아요.
**처방**: SQL 한 줄로 RLS 활성화 + 서비스 키만 접근 허용
**예상 시간**: 5분

### 2. Claude API 타임아웃 추가 (F-004, F-005)
**위험**: AI 서버가 느려지면 우리 서버도 같이 멈춥니다. 전화를 걸었는데 상대방이 안 받으면 영원히 기다리는 것과 같아요.
**처방**: 60초 타임아웃 설정 2곳
**예상 시간**: 10분

### 3. 활동지 미리보기 HTML 살균 (F-003)
**위험**: AI가 만든 HTML에 나쁜 코드가 섞일 수 있습니다. 배달 음식 포장을 열기 전에 소독하는 것처럼, HTML도 정화해야 합니다.
**처방**: DOMPurify 라이브러리 추가 또는 sandboxed iframe 사용
**예상 시간**: 30분

---

## 📈 건강 점수 산출

```
기본:        100
HIGH × 3:    -30  (F-001, F-002, F-003)
MEDIUM × 7:  -21  (F-004~F-010)
LOW × 8:     -8   (F-011~F-018)
npm audit:   -3   (high 2건)

최종:         78/100
```

---

## 📋 Phase별 요약

<details>
<summary>Phase 1: 빌드 & 테스트</summary>

- 빌드 성공 (모든 라우트 정상 컴파일)
- 340/340 테스트 통과
- TypeScript 에러 0건
- ESLint 미설정 (초기 설정 필요)
- npm audit: next.js DoS 4건 (breaking change 필요), picomatch ReDoS 2건

</details>

<details>
<summary>Phase 2: API 라우트 전수 점검</summary>

- 총 49개 API 라우트 검사
- CRITICAL: 0건
- HIGH: 1건 (TQ generate UUID 미검증)
- MEDIUM: 4건 (타임아웃 2, 수동 검증 2)
- LOW: 8건
- 모범 사례: chat, webhooks/stripe, payments/confirm, tickets — 완벽한 보안 패턴

</details>

<details>
<summary>Phase 3: DB/Supabase</summary>

- RLS: 대부분 테이블 적용, site_settings 누락 (HIGH)
- Migration: 001~048 확인, community_likes/comments 참조 불일치
- Service Role Key: 서버 전용 사용 확인, push/send 폴백 패턴 주의
- N+1: 심각한 패턴 미발견

</details>

<details>
<summary>Phase 4: 보안</summary>

- 하드코딩 시크릿: 없음
- XSS: dangerouslySetInnerHTML 6곳 (대부분 안전, 활동지 미리보기만 주의)
- 프롬프트 인젝션: stripControlTags + sanitizeUserInput으로 방어 완비
- 보안 헤더: 전체 적용 (CSP unsafe-inline은 개선 여지)
- 위기감지/출력 안전: 무결
- 결제: 3중 멱등성, 서버사이드 금액, HMAC 서명 검증

</details>

<details>
<summary>Phase 5: 코드 품질</summary>

- any 타입: 9건 (양호)
- console.log: 11건 (정리 권장)
- 미사용 export: ~92건 (주요: worksheet/schemas.ts, analytics.ts)
- 테스트: 340개이지만 API 라우트 통합 테스트 전무
- 에러 응답 중복: apiError() 헬퍼 도입 권장
- 번들: framer-motion 30개 import → motion 서브패스 전환 권장

</details>

---

*Generated by Nightwatch v1.0 — 2026-03-28 23:41 KST*
